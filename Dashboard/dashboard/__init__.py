"""Indonesia Indicators tracker.

Reads the datasets the collectors maintain under ``Dataset/`` and renders each
one as line charts, bucketed by the publisher and dataset it belongs to.  The
same figure code serves the Streamlit app (``Dashboard/app.py``) and the
offline HTML export (``Dashboard/export_html.py``).
"""
