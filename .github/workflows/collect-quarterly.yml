# Quarterly collection: reserved for BPS
#
# NOTE: GitHub fires `schedule` only from the repository's DEFAULT branch, and
# only from the copy of this file that lives there.  `main` is both the default
# and the branch the datasets live on, so the cron fires and lands where it
# should.  `workflow_dispatch` runs from any branch, and checks out and pushes
# TARGET_BRANCH regardless of which branch it was dispatched from.
#
# Every collector merges into the datasets and dedupes, so running twice is
# harmless: the second run finds nothing to do and writes nothing.
name: collect-quarterly

on:
  schedule:
    - cron: "0 4 20 1,4,7,10 *"   # 20 Jan/Apr/Jul/Oct 04:00 UTC = 11:00 WIB
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Report what would change, write nothing"
        type: boolean
        default: false
      full:
        description: "Re-read all history instead of only what is missing"
        type: boolean
        default: false

permissions:
  contents: write        # the run commits refreshed datasets back

concurrency:
  group: collect-quarterly
  cancel-in-progress: false   # two runs must never write the same files

env:
  # The branch the datasets live on, which is this repository's default.  Named
  # rather than inherited from the trigger so that a run dispatched from a
  # feature branch still commits its data here and nowhere else.
  TARGET_BRANCH: main

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 0        # a shallow clone cannot rebase before pushing

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: Collectors/requirements.txt

      - run: pip install -r Collectors/requirements.txt

      - name: Collect
        run: python Collectors/run.py quarterly $DRY_RUN $FULL
        env:
          DRY_RUN: ${{ inputs.dry_run && '--dry-run' || '' }}
          FULL: ${{ inputs.full && '--full' || '' }}

      - name: Commit refreshed datasets
        if: ${{ !inputs.dry_run }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -z "$(git status --porcelain Dataset)" ]; then
            echo "no dataset changes"; exit 0
          fi
          # Dataset only: a robot must never commit a stray build artefact.
          git add Dataset
          git commit -m "Quarterly collection: $(date -u +%Y-%m-%d)"
          # Rebase before pushing so a manual commit landing first does not
          # fail the run; retry because two schedules can just overlap.
          for attempt in 1 2 3; do
            git pull --rebase --autostash origin "$TARGET_BRANCH" \
              && git push origin HEAD:"$TARGET_BRANCH" && exit 0
            sleep $((attempt * 5))
          done
          exit 1
